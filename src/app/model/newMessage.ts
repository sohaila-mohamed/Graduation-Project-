
// tslint:disable-next-line:class-name
export interface newMessage {
  sender_id:number,
  receiver_id: number;
  msg_subject: string;
  is_readed: number;
  receiver_name: string;
  sender_name: string;
  msg_body: string;
  fcm_token:String;
  }
